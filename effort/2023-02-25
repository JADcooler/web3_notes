oracle
______

so..
we call a oracle contract, which in turn relies on binance API
to fetch prices.

we have a mapping of Ids to bool for requests made.

if Id match and caller is oracle.
we update prices.

In oracle side, we have the Api stuff and 
a pendingRequests mapping which handles similar.
